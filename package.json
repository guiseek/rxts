{
  "name": "rxts",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "postbuild": "tsc lib/*.ts --declaration --emitDeclarationOnly --removeComments --downlevelIteration --outFile dist/rxts.d.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/jest": "^27.5.0",
    "@types/node": "^17.0.31",
    "jest": "^28.1.0",
    "sass": "^1.51.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.7"
  },
  "workspaces": [
    "packages/eventable"
  ]
}
